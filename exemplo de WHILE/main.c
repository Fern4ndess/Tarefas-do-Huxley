#include <stdio.h>

int main() {
    int num = 0;

    // Loop até que o valor de num seja 5
    while (num < 5) {
        printf("O valor atual de num é %d\n", num);
        num++;
    }

    return 0;
}
